<div class="note">
    <h1>By {{f_name user}}</h1>
    <div class="body">{{body}}</div>

    <h1>Notes</h1>

    {{#each notes}}
    <h2>By {{f_name user}}</h2>
    <div class="body">{{body}}</div>
    {{/each}}
</div>